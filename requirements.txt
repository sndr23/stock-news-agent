# 依赖版本约束（2026-08-02 起添加上限：防 breaking change 突然击穿 CI/生产）
# 本文件覆盖当前已实测的运行版本（见各行的 ~= 语义）。
# 原则: 允许小版本内修复更新(~= 表示 >=已知 且 <下一个 minor)，
#       阻止跨 minor 的重大升级（akshare 列名 / langchain 接口漂移风险）。
python-dotenv>=1.0.0,<2.0.0
pydantic~=2.12.0
httpx~=0.28.0
akshare~=1.18.0
requests~=2.32.0
pandas~=2.3.0
# 交易日判断（2026-08-13 与 requirements-cloud.txt 对齐：本地 RT_ALWAYS_ON=0 时节假日判断依赖它）
chinese_calendar>=1.9.0,<2.0.0