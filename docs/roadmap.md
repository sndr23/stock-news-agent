# Roadmap（2026-08-23）

> 权威职责：阶段顺序 / 阶段目标 / Stage Gate。由 Coordinator 维护。

## 阶段总览

| Stage | 名称 | 状态 | 目标 |
|---|---|---|---|
| 1 | 全链路跑通 | ✅ DONE | 8 新闻源 + 信号源 → 去重 → LLM 判定 → 微信推送；创业板择时上线 |
| 2 | **生产稳定 + 数据源加固** | 🔵 当前 | 核心数据点从免费抓取升级为带鉴权稳定接口；修复已知复杂度风险 |
| 3 | 样本外验证 | ⏳ 规划 | 对择时系统做严格样本外（OOS）回测，量化 in-sample 过拟合程度 |
| 4 | 系统扩展 | ⏳ 规划 | 待 Stage 3 结论后再定（如多资产/截面验证、持仓错配缓解） |

## Stage 2 详细目标与 Gate

### 目标
1. **P0 数据源加固**：399006 日线、中际旭创日线、创业板50 TTM PE 接入 Tushare Pro（token 缺失自动降级免费源）。
2. **P1 数据源加固**：期货基差（东财被反爬的对冲）+ 资金流替代通道。
3. **P2 数据源加固**：外盘（Stooq/雅虎双源冗余）+ 估值（Tushare index_dailybasic）。
4. **复杂度治理**：`real_time_push.py` 中 `_is_same_event`（L803-962，160 行/12 分支）降低回归风险。
5. 每项改动必须有回测/验证证据，精度不低于当前基线。

### Stage 2 Gate（全部满足才进入 Stage 3）
- [ ] SNA-01（P0 接入）合入 main 且 main 复验通过
- [ ] 数据源健康度监控可用（连续 N 轮付费源失败 → 推送告警）
- [ ] 回归验证：`pytest -m unit` 全绿 + 择时系统回测数字不低于基线（+188.8% 口径）
- [ ] 无 OPEN 的 BLOCKED Task / ND 决策
- [ ] 文档同步：status / roadmap / task-board / 数据源加固方案

## Stage 3 目标（预规划，Gate 通过后细化）

- 用 2026-08-22 之后或留出的样本做 OOS 回测；
- 明确量化"过拟合上限口径"：如果 OOS 超额显著衰减，则进入调参护栏流程（不直接改权重）。
