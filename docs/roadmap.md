# Roadmap（2026-08-28）

> 权威职责：阶段顺序 / 阶段目标 / Stage Gate。由 Coordinator 维护。

## 阶段总览

| Stage | 名称 | 状态 | 目标 |
|---|---|---|---|
| 1 | 全链路跑通 | ✅ DONE | 8 新闻源 + 信号源 → 去重 → LLM 判定 → 微信推送；创业板择时上线 |
| 2 | **生产稳定 + 数据源加固** | 🔵 当前 | 核心数据点采用免费多源回退和缓存；修复已知复杂度风险 |
| 3 | 样本外验证 | 🔵 首轮已完成 | 已完成滚动训练/测试验证器；继续等待 2026-08-22 之后留出样本 |
| 4 | 系统扩展 | ⏳ 规划 | 待 Stage 3 结论后再定（如多资产/截面验证、持仓错配缓解） |

## Stage 2 详细目标与 Gate

### 目标
1. **P0 数据源加固**：399006 日线、中际旭创日线、创业板50 TTM PE 使用免费接口与本地缓存。
2. **P1 数据源加固**：期货基差（新浪→中金所免费链）+ 资金流东财免费接口。
3. **P2 数据源加固**：外盘免费源稳定性 + 免费估值源稳定性。
4. **复杂度治理**：`real_time_push.py` 中 `_is_same_event`（L803-962，160 行/12 分支）降低回归风险。
5. 每项改动必须有严格信息集下的回测/验证证据；不得为了追赶历史宽松口径而调参。

### Stage 2 Gate（全部满足才进入 Stage 3）
- [x] SNA-01（P0 接入）合入 main 且 main 复验通过
- [x] SNA-02（P1 接入）免费期货/资金流链路完成并通过专项回归
- [x] SNA-03（P2 接入）外盘 Yahoo/Stooq 回退与 PE 稳定性完成；线上外部站点可达性仍按失败降级
- [x] SNA-04（复杂度治理）去重规则行为等价重构并通过专项回归
- [x] 数据源健康度监控可用（连续 3 轮免费数据维度失败 → 推送一次告警；恢复后可再次触发）
- [x] 回归验证：`python -m pytest tests/ -q -m "unit" -p no:cacheprovider` 全绿 + 择时系统回测信息集无未来函数、结果可复现并如实记录（当前严格参考：策略 +105.3%，买入持有 +170.1%）
- [x] 无 OPEN 的 BLOCKED Task / ND 决策
- [x] 文档同步：status / roadmap / task-board / 数据源加固方案

### SNA-05 验收记录（2026-08-28）

- 健康度按最终数据维度统计，不区分内部具体使用了哪一个免费回退源；拿到有效结果即视为该维度成功。
- 连续失败达到 3 轮才告警，成功恢复后失败计数和告警锁存清零；推送失败不锁存，下一轮继续尝试。
- `--collect` 持续更新快照并允许健康告警，但不恢复普通因子异动推送；`--dry-run` 为纯只读模式。
- 定向回归和完整 unit 门禁已覆盖，当前工作区结果为 `1160 passed, 21 deselected`（`-p no:cacheprovider`）。

## Stage 3 当前结果与后续

- 首轮 3年训练/1年测试、9折 rolling OOS：策略复合 +64.6%，买入持有 +111.2%；策略最大回撤 -27.8%，买入持有 -57.0%。
- 单次换仓成本 0.3% 时 OOS 复合 +19.0%，年化 +2.0%，夏普 0.20，最大回撤 -38.3%；参数跨折漂移，暂不接入任何新参数。
- 下一验证窗口使用 2026-08-22 之后或事先留出的样本；在留出样本形成前不继续针对全历史调权重/档位线。
- 盘中快照验收：生产状态新增可审计快照归档；`scripts/backtest_intraday_snapshots.py` 只用 d-1 完整日线 + d 快照评分，主收益按 d 收盘成交到 d+1 完整收盘计算，另保留快照到下一快照诊断。默认要求评估区间不缺交易日，真实归档积累后再形成独立的盘中回测基线。
- 生产观察前置检查：确认外部定时服务持续触发 `realtime-factor.yml`，GitHub schedule 14:15 兜底已启用，并确认 `factor_state` 按交易日更新；另确认 `chinext-timing.yml` 的 cron-job.org 14:45 主触发；快照过期时修正层必须保持关闭。
- 状态配置前置检查：`GIST_TOKEN` 与 `GIST_ID` 必须成对配置；半配置会直接中止，避免本地旧状态冒充云端状态。
